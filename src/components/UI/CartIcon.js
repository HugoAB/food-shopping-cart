import React from "react";
import cartIcon from "../../assets/cart_icon.png"

const CartIcon = (props) => {
  return (
    <img style={{width: "34px"}} src={cartIcon} />
  );
};

export default CartIcon;